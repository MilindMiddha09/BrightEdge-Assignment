services:
  log-analyzer:
    build:
      context: .
      dockerfile: Dockerfile
    image: mysql-log-analyzer:latest
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
